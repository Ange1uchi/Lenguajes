#include "vector3.hpp"
#include <cassert>
#include <iostream>

//int main() {
 //   Vector3 a(1, 0, 0);
//    Vector3 b(0, 1, 0);
//    Vector3 c(0, 0, 1);

    // Producto cruz
//    Vector3 cross = a * b; // debe ser (0, 0, 1)
//    assert(cross.equals(c));

    // Producto punto
//    double dot = a % b;
//    assert(dot == 0.0);

    // Norma
//    double normA = &a;
 //   assert(std::fabs(normA - 1.0) < 1e-6);

    // Operaciones con escalares
//    Vector3 sumScalar = b + 3.0; // (3, 4, 3)
 //   assert(sumScalar.equals({3, 4, 3}));

 //   Vector3 expr = (b + b) * (c - a);
  //  std::cout << "Todo funciona correctamente " << std::endl;
 //   return 0;
//}
